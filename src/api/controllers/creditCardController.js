const Project = require("../models/projectModel");
const mongoose = require('mongoose');
const Meeting = require("../models/meetingModel");
const User = require("../models/userModel");

const addCreditCardInfo = (params) => {
  
}


module.exports = {
  addCreditCardInfo
};

